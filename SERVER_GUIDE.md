# 自行開設多人伺服器

《霧落農歌：鐘塔之季》v1.1.2 使用 Godot ENet／UDP，可像常見沙盒遊戲一樣由玩家自行開設世界，不依賴官方帳號、雲端或媒合服務。單人離線模式不會建立網路端點。

## 最快開始

### 遊戲內主機

1. 按 `M` 或手把 `Select／Back` 打開「霧落連線世界」。
2. 填寫玩家、世界與伺服器名稱，選擇農場制度與關係制度；預設 UDP 埠為 `27180`。
3. 按「開設主機」。同一區域網路的朋友輸入主機區網 IP 和相同連接埠即可加入。

### 專用伺服器

雙擊發行包中的 `Start Dedicated Server.cmd`。同一個正式 EXE 會以無視窗、無顯示卡需求的專用伺服器模式啟動。進階命令如下：

```text
Mistfall-Bell-Seasons.exe --headless -- --server --port=27180 --max-clients=8 --server-name="Mistfall Dedicated" --world=mistfall --farm-mode=competitive --relationship-mode=competitive
```

- 玩家上限可設 1–16 人。
- 世界名稱只接受英數、底線與連字號，最長 32 字元。
- 世界資料會原子寫入 Godot `user://pixelrpg_servers/<world>.json`，上一版保留為 `.bak`。
- 客戶端與伺服器必須使用相同的 `1.1.0` 通訊協定；不相容版本會在握手階段被拒絕。

## 世界制度

農場可在建立世界時選擇：

- `shared`／共同整合：所有玩家共用農地、倉庫與經濟，適合分工合作。
- `private`／各自獨立：每個本機玩家識別碼都有自己的農場、工具、金幣、漁獲與進度，不互相覆寫。
- `competitive`／各自競賽：保留私人農場，另依收入、收成、農場等級與開墾量產生即時排行。

關係可選 `independent`（每人的友情、戀愛、婚姻與家庭彼此獨立）或 `competitive`（同一 NPC 會有追求排行）。競爭模式允許多名玩家交往，但求婚者必須好感最高；若第一名平手，下一場村莊競賽事件前不能取得優先求婚權，已締結婚約的 NPC 不會再接受別人求婚。

多人主線會依在線人數切換：1 人「獨鐘守望」、2 人「雙鐘盟約」、3–4 人「四季合奏」、5 人以上「霧落拓荒議會」。農場與關係制度會進一步改寫篇章目標與台詞；人數變更事件會保存在世界檔。

## 從網際網路加入

ENet 使用 UDP。主機需在 Windows 防火牆與路由器設定中允許並轉送所選 UDP 埠（預設 `27180`）至伺服器電腦；朋友輸入主機的公開 IP。若 ISP 使用 CGNAT，請改用具有 UDP 轉送能力的 VPN／虛擬區網或向 ISP 申請可轉送的位址。

## 安全界線

- 首版適合與信任的朋友或在 LAN 使用；沒有官方帳號、公開伺服器目錄、管理後台或聊天紀錄。
- ENet 連線本身不提供 TLS 內容加密；不要把常用密碼、真實姓名或私密資訊當作玩家／伺服器名稱。
- 伺服器驗證通訊協定、名字長度、地圖、輸入速率、序號、移動向量、操作白名單與 payload 大小；伺服器才是移動與共同農場狀態的權威來源。
- 若不需要對外連線，不要設定路由器轉送；單人模式與只在 LAN 遊玩不需公開 UDP 埠。

## 已同步範圍

v1.1.2 同步玩家名稱、位置、朝向、所在地圖，以及日曆、天氣、農地、採集、釣魚、出貨、動物照料、NPC 交談、戀愛競爭、任務、洞窟與深潮世界狀態。私人／競賽農場以本機產生的 128-bit 玩家識別碼分隔；沒有帳號驗證，因此仍以信任的朋友為使用前提。伺服器每秒 20 次傳送位置快照、每 0.5 秒同步各玩家可見的世界視圖，並每 30 秒及關服時保存。網路協定仍為相容的 `1.1.0`；發行閘門以 1 台伺服器＋16 個客戶端驗證最大人數。

問題回報請附伺服器與客戶端版本、UDP 埠、拓撲（LAN／網際網路）、重現步驟與去除個資後的日誌。
